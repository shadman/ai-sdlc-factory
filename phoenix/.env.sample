# No secrets required — Phoenix runs fully open by default.
# Set this in your agents-api .env to point at this Phoenix instance:
#
#   PHOENIX_ENDPOINT=http://localhost:4317
#
# Phoenix UI is available at: http://localhost:6006
